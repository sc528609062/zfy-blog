# zfy-blog · 开发计划书 v3

> 项目名称：**zfy-blog**  
> 文档版本：v3.0  
> 拟定日期：2026-05-11  
> 项目性质：从零开发的综合 CMS / 内容商业化平台  
> 技术主线：**Laravel + MySQL + Redis + Blade + Livewire + REST API**

> 实施记录（2026-09-13）：此文档保留最初需求。当前运行环境为最低 PHP 8.2 / MySQL 5.7，推荐 PHP 8.4 / MySQL 8.4；后台实际使用 Vue 3 + Element Plus + Vue Router + Pinia。实现、测试与尚未验收的差异见 [功能验收矩阵](docs/implementation-status.md)，依赖版本以锁文件为准。

---

## 一、项目总览

### 1.1 项目定位

**zfy-blog** 不是轻量个人博客，而是一套面向个人站长、内容作者和资源运营者的完整 CMS 平台。

系统核心目标：

- 管理和展示 **文章、图集、资源、独立页面**。
- 支持 **多作者投稿、审核、作者主页、收益分成、提现结算**。
- 支持 **VIP、多级会员、付费文章、付费资源、余额钱包、积分体系、积分商城**。
- 支持 **公开 REST API、主题上传、插件扩展、页面构建器、媒体库、菜单管理、评论互动、通知系统**。
- 支持宝塔单机部署，也提供 Docker Compose 开发 / 测试方案。

### 1.2 与旧计划书的关系

当前仓库只有 `开发计划书.md` 和参考截图，不再按旧文档中的 Next.js / Prisma / Steam 资源站方案推进。

本版计划书将旧方案整体替换为：

- **从零开发**，不基于旧 Next.js 项目重构。
- 项目名称固定为 **zfy-blog**。
- 技术栈固定为 **Laravel + PHP + MySQL + Redis**。
- 内容模型从 `GAME / PHOTO / PAGE` 改为统一内容模型：`post / images / files / page`。
- `files` 是前台资源下载内容页；媒体库是后台统一文件资产管理，两者彻底分离。
- 支付、VIP、积分、主题、插件、页面构建器进入首版规划。

### 1.3 用户与角色

| 身份 | 说明 | 主要能力 |
| --- | --- | --- |
| 游客 | 未登录访问者 | 浏览公开内容、搜索、查看公开评论、注册登录 |
| USER | 普通用户 | 评论、点赞、收藏、购买内容、购买 VIP、下载授权资源、个人中心 |
| AUTHOR | 作者身份 | 投稿、管理自己的内容、查看收益、申请提现 |
| EDITOR | 内容编辑 | 审核投稿、管理内容、评论、分类、标签、媒体 |
| ADMIN | 管理员 | 站点运营配置、用户管理、订单管理、主题插件、页面构建 |
| SUPER_ADMIN | 超级管理员 | 全部权限、支付密钥、系统升级、插件安全、管理员管理 |

> 后台角色采用 `SUPER_ADMIN / ADMIN / EDITOR / USER` 四级权限；`AUTHOR` 是用户扩展身份，允许普通用户申请成为作者。VIP 是会员等级，不等同于后台角色。

### 1.4 首版边界

首版按“一次做全”的完整平台规划，但实施时分阶段交付。核心原则：

- 内容、用户、权限、支付、订单、媒体、编辑器、主题、插件、页面构建器都进入架构设计。
- 每个复杂模块必须有明确的数据模型、权限边界、失败处理和测试场景。
- 不依赖 WordPress / Typecho / zibll 代码，只参考其产品体验与运营逻辑。

---

## 二、技术架构

### 2.1 技术栈

| 层级 | 选型 | 说明 |
| --- | --- | --- |
| 后端框架 | Laravel 12 | 主应用框架 |
| PHP | PHP 8.3+ | 生产建议 PHP 8.3 或更高 |
| 数据库 | MySQL 8 | 主数据存储，InnoDB，utf8mb4 |
| 缓存 / 队列 | Redis | 缓存、限流、队列、会话可选 |
| 前台渲染 | Blade | SEO 友好的服务端渲染 |
| 后台交互 | Blade + Livewire | 管理后台表单、列表、弹窗、上传、拖拽 |
| API 认证 | Laravel Sanctum | 公开 REST API Token |
| 搜索 | Laravel Scout | 默认 MySQL FULLTEXT + Redis 缓存，可选 Meilisearch |
| 前端构建 | Vite | 后台编辑器、主题资源、页面构建器资源打包 |
| 编辑器 | TipTap / ProseMirror | 块编辑 + 富文本 + Markdown |
| 样式 | Tailwind CSS | 前后台基础样式体系 |
| 队列任务 | Laravel Queue | 支付回调后处理、搜索同步、邮件、通知 |
| 定时任务 | Laravel Scheduler | 订单超时、VIP 过期、索引修复、数据清理 |
| 文件存储 | Laravel Filesystem | Local / S3 / OSS / COS / MinIO |
| 测试 | PHPUnit / Pest + Laravel Dusk | 单元、功能、浏览器测试 |

### 2.2 核心依赖建议

```json
{
  "php": "^8.3",
  "laravel/framework": "^12.0",
  "laravel/sanctum": "^4.0",
  "laravel/scout": "^10.0",
  "livewire/livewire": "^3.0",
  "league/flysystem-aws-s3-v3": "^3.0",
  "predis/predis": "^2.0",
  "meilisearch/meilisearch-php": "^1.0",
  "spatie/laravel-permission": "^6.0",
  "spatie/laravel-medialibrary": "^11.0",
  "spatie/laravel-activitylog": "^4.0",
  "mews/purifier": "^3.0"
}
```

前端依赖：

```json
{
  "@tiptap/core": "^2.x",
  "@tiptap/starter-kit": "^2.x",
  "@tiptap/extension-image": "^2.x",
  "@tiptap/extension-table": "^2.x",
  "@tiptap/extension-code-block-lowlight": "^2.x",
  "alpinejs": "^3.x",
  "sortablejs": "^1.x",
  "tailwindcss": "^3.x"
}
```

### 2.3 分层结构

```text
app/
  Actions/              # 用例动作，如 PublishContent、CreateOrder
  Console/              # 定时任务
  Domain/               # 领域模块
    Content/
    Media/
    Commerce/
    Vip/
    Wallet/
    Points/
    Author/
    Theme/
    Plugin/
    PageBuilder/
    Notification/
  Http/
    Controllers/
      Web/
      Api/V1/
      Admin/
    Middleware/
  Livewire/             # 后台与交互组件
  Models/
  Policies/
  Services/
  Support/
bootstrap/
config/
database/
resources/
  views/
    themes/
    admin/
    install/
  js/
    editor/
    page-builder/
routes/
  web.php
  api.php
  admin.php
  install.php
storage/
themes/                 # 上传主题包解压目录
plugins/                # 上传插件包解压目录
```

---

## 三、信息架构

### 3.1 前台路由

| 路径 | 说明 |
| --- | --- |
| `/` | 首页，综合信息流，可由页面构建器控制 |
| `/posts` | 文章频道 |
| `/images` | 图集频道 |
| `/files` | 资源频道 |
| `/c/{slug}` | 分类聚合页 |
| `/tag/{slug}` | 标签聚合页 |
| `/content/{slug}` | 统一内容详情页，根据类型切换模板 |
| `/p/{slug}` | 独立页面 |
| `/search` | 搜索页 |
| `/rank` | 排行榜 |
| `/vip` | 会员中心 / 会员购买页 |
| `/points-store` | 积分商城 |
| `/authors` | 作者列表 |
| `/author/{username}` | 作者主页 |
| `/links` | 友情链接 |
| `/login` | 登录 |
| `/register` | 注册 |
| `/forgot-password` | 找回密码 |
| `/user` | 用户中心 |
| `/user/orders` | 我的订单 |
| `/user/downloads` | 我的下载 |
| `/user/wallet` | 钱包与余额 |
| `/user/points` | 积分明细 |
| `/user/vip` | 我的会员 |
| `/user/author` | 作者工作台 |

### 3.2 后台路由

```text
仪表盘
  /admin

内容
  /admin/contents
  /admin/contents/create
  /admin/contents/{id}/edit
  /admin/categories
  /admin/tags
  /admin/comments
  /admin/reports

媒体
  /admin/media
  /admin/media/folders
  /admin/storage

作者
  /admin/authors
  /admin/submissions
  /admin/earnings
  /admin/withdrawals

商业化
  /admin/orders
  /admin/payments
  /admin/wallets
  /admin/points
  /admin/points-store
  /admin/vip-levels
  /admin/download-rules

用户
  /admin/users
  /admin/roles
  /admin/permissions

外观
  /admin/themes
  /admin/themes/install
  /admin/menus
  /admin/page-builder
  /admin/widgets

扩展
  /admin/plugins
  /admin/plugins/install
  /admin/hooks

系统
  /admin/settings
  /admin/seo
  /admin/mail
  /admin/wechat
  /admin/security
  /admin/audit-logs
  /admin/installer
  /admin/updater
```

### 3.3 菜单系统

菜单能力参考 WordPress：

- 支持多套菜单：主导航、移动端导航、页脚导航、用户中心导航、自定义位置。
- 菜单项支持内部内容、分类、标签、独立页面、外部链接、自定义锚点。
- 支持拖拽排序、层级菜单、打开方式、图标、权限可见性。
- 主题可声明菜单位置，后台按当前主题展示可配置区域。

---

## 四、内容模型

### 4.1 统一内容类型

| 类型 | 说明 | 是否进入首页信息流 | 是否可付费 | 是否可评论 |
| --- | --- | --- | --- | --- |
| `post` | 普通文章 | 是 | 是 | 可配置 |
| `images` | 瀑布流图集 | 是 | 是 | 可配置 |
| `files` | 资源下载页 | 是 | 是 | 可配置 |
| `page` | 独立页面 | 否 | 一般否 | 默认关闭 |

### 4.2 内容状态

```text
draft       草稿
pending     待审核
published   已发布
private     私密
scheduled   定时发布
rejected    审核拒绝
trash       回收站
```

### 4.3 核心字段

`contents` 表建议字段：

| 字段 | 说明 |
| --- | --- |
| `id` | 主键 |
| `type` | `post / images / files / page` |
| `status` | 内容状态 |
| `title` | 标题 |
| `slug` | URL slug，唯一 |
| `excerpt` | 摘要 |
| `cover_media_id` | 封面媒体 |
| `author_id` | 作者 |
| `editor_id` | 最近审核 / 编辑人 |
| `block_json` | TipTap 块源数据 |
| `rendered_html` | 前台渲染缓存 HTML |
| `markdown_cache` | Markdown 源码缓存 |
| `plain_text` | 搜索用纯文本 |
| `visibility` | 公开、登录可见、VIP 可见、密码可见 |
| `price` | 单独购买价格 |
| `vip_discount_type` | VIP 折扣类型：比例 / 固定金额 |
| `vip_discount_value` | VIP 折扣值 |
| `required_vip_level_id` | 所需 VIP 等级 |
| `comment_policy` | 评论策略 |
| `download_policy` | 下载策略 |
| `seo_title` | SEO 标题 |
| `seo_description` | SEO 描述 |
| `seo_keywords` | SEO 关键词 |
| `published_at` | 发布时间 |
| `created_at` / `updated_at` | 时间戳 |

### 4.4 图集内容

`images` 类型额外能力：

- 每条内容可绑定多张图片，按 `sort_order` 排序。
- 前台默认瀑布流展示，点击进入灯箱。
- 支持图片标题、说明、版权、可见权限。
- 支持批量上传、拖拽排序、封面选择。
- 可配置是否允许下载原图。

### 4.5 资源内容

`files` 类型是“资源下载页”，不是媒体库本身。

资源页能力：

- 富文本介绍、封面、截图、版本号、文件大小、更新时间。
- 多下载源：本地附件、对象存储、外链、网盘链接。
- 网盘字段：平台、URL、提取码、解压密码、备注。
- 权限字段：公开、登录、VIP、购买、评论可见、密码可见、积分兑换。
- 下载限制：每日次数、总次数、IP 限制、设备限制、防盗链、日志记录。

### 4.6 独立页面

`page` 类型用于：

- 关于本站
- 隐私政策
- 用户协议
- 会员说明
- 投稿说明
- 友情链接说明
- 自定义落地页

独立页面不进入首页和频道信息流，只通过菜单、页面构建器或直链访问。

---

## 五、编辑器系统

### 5.1 编辑器定位

编辑器是 zfy-blog 的核心生产力模块，目标是融合：

- WordPress Gutenberg 的块编辑体验。
- Typecho / Markdown 编辑器的快捷写作体验。
- 稀土掘金富文本编辑器的内容创作体验。
- 用户提供截图中的完整工具栏能力。

### 5.2 三模式一体

| 模式 | 说明 |
| --- | --- |
| 块编辑器 | 默认模式，内容由可拖拽、可配置的 block 组成 |
| 富文本 | 类传统编辑器工具栏，可直接排版、插入媒体、插入短代码 |
| Markdown | 支持 Markdown 源码编辑，并可转换为 block JSON |

切换规则：

- `block_json` 是权威源数据。
- 保存时生成 `rendered_html`，供前台渲染和 SEO 使用。
- Markdown 模式生成 `markdown_cache`，切回块编辑器时转换为 block。
- 无法无损转换的复杂块，以自定义 fenced block 或短代码形式保留。

### 5.3 首版块类型

基础块：

- 段落
- 标题 H1-H6
- 加粗、斜体、删除线、行内代码
- 引用
- 分割线
- 有序列表
- 无序列表
- 待办列表
- 表格
- 代码块
- 文字颜色
- 时间
- 缩进
- 特殊符号
- Emoji 表情

媒体块：

- 单图
- 图集
- 音频
- 视频
- Bilibili
- 外部 iframe
- 播放器
- 歌单

内容增强块：

- 消息提示
- 进度条
- 备注
- 标签栏
- 列表卡片
- 卡片 1 / 卡片 2
- 时间轴
- 折叠内容
- 点击复制
- 彩色分割线
- 跑马条
- 多彩按钮
- 便条按钮
- 隐藏内容

资源块：

- 文件下载
- 网盘下载
- 付费内容提示
- VIP 可见提示
- 登录可见提示
- 评论可见提示
- 密码可见提示

### 5.4 安全与渲染

- 前台不直接信任编辑器 HTML。
- 保存时服务端使用 HTML Purifier 清洗。
- 自定义 block 由服务端 renderer 白名单渲染。
- iframe 仅允许后台配置白名单域名。
- SVG 默认禁止直接上传或作为图片渲染，避免 XSS。
- 隐藏内容块在服务端按权限裁剪，不只依赖前端隐藏。

---

## 六、媒体库与存储

### 6.1 媒体库定位

媒体库统一管理站点所有文件资产，包括：

- 图片
- 图集图片
- 视频
- 音频
- 压缩包
- 文档
- 资源文件
- 主题资源
- 插件资源

媒体库不等同于 `files` 内容。`files` 是资源详情页，媒体库是底层文件资产。

### 6.2 存储驱动

| 驱动 | 用途 |
| --- | --- |
| Local | 默认本地存储，适合开发和小站 |
| S3 | AWS S3 / Cloudflare R2 / MinIO |
| OSS | 阿里云 OSS |
| COS | 腾讯云 COS |

后台配置项：

- 驱动类型
- Access Key / Secret Key
- Bucket
- Region
- Endpoint
- CDN 域名
- 是否私有 Bucket
- 签名 URL 有效期
- 上传大小限制
- MIME 白名单

### 6.3 文件安全

- 校验 MIME 和文件扩展名。
- 图片使用服务端解码校验。
- 压缩包限制层级和总大小，避免压缩炸弹。
- 私有资源走授权下载路由，不直接暴露真实路径。
- 下载地址支持一次性签名、防盗链、过期时间和下载日志。

---

## 七、用户、VIP 与权限

### 7.1 用户账户

用户支持：

- 用户名 / 邮箱 / 手机号登录。
- 邮箱验证。
- 找回密码。
- 第三方登录预留：微信、QQ、GitHub。
- 头像、昵称、个人简介、主页链接。
- 用户中心。
- 登录设备和登录日志。

### 7.2 VIP 体系

VIP 独立于后台角色。

`vip_levels`：

| 字段 | 说明 |
| --- | --- |
| `name` | 等级名称，如 VIP、SVIP、永久会员 |
| `level` | 等级排序，数字越大权限越高 |
| `duration_days` | 有效期天数，永久为 null |
| `price` | 默认售价 |
| `discount_rate` | 全局折扣比例 |
| `discount_amount` | 固定优惠金额 |
| `download_limit_daily` | 每日下载次数 |
| `benefits` | 权益 JSON |
| `enabled` | 是否启用 |

用户会员记录：

- 支持多次续费。
- 支持升级补差价策略。
- 支持后台手动开通、支付开通、卡密开通预留。
- 过期后自动降级为普通用户。

### 7.3 内容权限

内容可配置：

- 游客可见。
- 登录可见。
- 指定 VIP 等级可见。
- 单独购买可见。
- 积分兑换可见。
- 评论后可见。
- 密码可见。
- 作者 / 管理员可见。

权限判断统一由 `ContentAccessService` 处理，前台、API、下载接口、编辑预览都必须使用同一套规则。

---

## 八、商业化系统

### 8.1 交易模型

系统同时支持：

- 直接支付订单。
- 余额钱包。
- 积分体系。
- 积分抵扣。
- 积分商城。
- VIP 购买。
- 单内容购买。
- 资源下载购买。

### 8.2 订单类型

```text
vip_purchase        购买 VIP
content_purchase    购买文章 / 图集 / 资源
wallet_recharge     余额充值
points_purchase     积分购买
points_exchange     积分商城兑换
author_withdrawal   作者提现
```

订单状态：

```text
pending     待支付
paid        已支付
closed      已关闭
refunded    已退款
failed      支付失败
cancelled   用户取消
```

### 8.3 支付网关

首版支持：

- 支付宝官方
- 微信支付官方
- 虎皮椒 V3
- 易支付

支付设计要求：

- 所有网关实现统一 `PaymentGateway` 接口。
- 支付参数、密钥、回调 URL 在后台配置。
- 回调必须做签名验证。
- 回调处理必须幂等。
- 支持主动查单。
- 支持支付超时自动关闭。
- 支持支付日志和异常日志。
- 支持 PC 扫码、移动 H5、公众号内 JSAPI 预留。

> 微信通知和微信 JSAPI 支付需要区分。微信支付依赖商户号；公众号通知 / OpenID 绑定通常需要已认证服务号能力。没有微信条件时，系统仍可使用站内信、邮件和其他支付渠道。

### 8.4 钱包与余额

钱包能力：

- 余额充值。
- 余额支付。
- 余额退款。
- 交易流水。
- 管理员调账。
- 冻结金额预留。
- 充值赠送规则预留。

### 8.5 积分体系

积分来源：

- 签到。
- 评论。
- 投稿。
- 内容发布。
- 购买内容。
- 购买 VIP。
- 管理员奖励。

积分用途：

- 购买内容时抵扣。
- 积分商城兑换资源 / VIP / 优惠券。
- 用户等级展示。
- 排行榜。

积分必须有流水，任何变动不可只改用户总数。

---

## 九、多作者与收益

### 9.1 作者入驻

用户可申请作者身份，字段包括：

- 真实姓名或昵称。
- 联系方式。
- 创作方向。
- 收款信息。
- 申请说明。

管理员审核通过后，用户获得作者工作台。

### 9.2 投稿流程

投稿状态：

```text
draft       作者草稿
pending     提交审核
rejected    审核拒绝
published   发布
revision    修改后复审
```

审核策略由后台配置：

- 所有投稿必须审核。
- 指定作者免审。
- 指定分类必须审核。
- 命中敏感词必须审核。
- 历史违规用户必须审核。

### 9.3 收益分成

平台统一收款，按内容作者和分成规则记录收益。

分成规则优先级：

1. 内容单独分成比例。
2. 作者单独分成比例。
3. 分类分成比例。
4. 全站默认分成比例。

收益状态：

```text
pending       待结算
available     可提现
withdrawing   提现中
paid          已打款
cancelled     已取消
```

### 9.4 提现

首版采用后台人工提现：

- 作者提交提现申请。
- 管理员审核。
- 管理员线下打款。
- 后台填写打款凭证并标记完成。
- 系统记录提现日志和操作日志。

---

## 十、评论、互动与通知

### 10.1 评论系统

评论能力：

- 一级评论。
- 楼中楼回复。
- 评论点赞。
- 评论举报。
- 评论审核。
- 敏感词过滤。
- 黑名单。
- 管理员置顶 / 删除。
- 站内信和邮件通知。

审核策略可配置：

- 全部审核。
- 首次评论审核。
- 命中敏感词审核。
- 指定内容类型审核。
- 作者自行审核自己内容下的评论。

### 10.2 互动能力

- 点赞。
- 收藏。
- 浏览记录。
- 下载记录。
- 内容举报。
- 用户关注作者预留。

### 10.3 通知渠道

必做：

- 站内信。
- 邮件。

可选：

- 微信公众号通知。

微信通知启用条件：

- 已认证服务号。
- 配置 AppID / AppSecret。
- 配置服务器 Token / EncodingAESKey。
- 配置网页授权域名。
- 用户绑定 OpenID。
- 配置模板消息或订阅通知模板 ID。

无微信条件时，系统自动降级到站内信和邮件。

---

## 十一、搜索系统

### 11.1 默认搜索

默认使用 MySQL FULLTEXT + Redis 缓存：

- 标题。
- 摘要。
- 正文纯文本。
- 分类。
- 标签。
- 作者。
- 文件资源元信息。

### 11.2 Meilisearch 可选增强

启用 Meilisearch 后：

- 使用 Laravel Scout 同步索引。
- 支持更好的中文搜索体验。
- 支持权重、筛选、排序、拼写容错。
- 索引同步失败进入队列重试。
- 后台提供“重建索引”按钮。

### 11.3 搜索权限

搜索结果必须遵守内容可见权限：

- 私密内容不进入公开搜索。
- 付费内容可显示标题和摘要，但隐藏正文和下载地址。
- VIP 内容可按配置显示预览。

---

## 十二、主题系统与页面构建器

### 12.1 主题包

支持后台上传主题包。

主题包必须包含：

```text
theme.json
views/
assets/
preview.png
```

`theme.json` 示例：

```json
{
  "name": "Default",
  "slug": "default",
  "version": "1.0.0",
  "author": "zfy-blog",
  "compatible": "^3.0",
  "entry": "views/layout.blade.php",
  "preview": "preview.png",
  "menus": ["primary", "mobile", "footer"],
  "regions": ["home", "sidebar", "footer"],
  "settings_schema": {}
}
```

安全要求：

- 禁止主题包覆盖核心文件。
- 禁止执行任意 PHP 安装脚本。
- 只允许加载主题声明的 Blade 视图和静态资源。
- 上传 zip 解压前检查路径穿越。
- 主题启用前检查兼容版本。

### 12.2 页面构建器

页面构建器用于搭建：

- 首页。
- 频道页。
- 独立页面。
- 专题页。
- 落地页。

构建器能力：

- 行 / 列布局。
- 响应式配置。
- 内容流模块。
- 分类模块。
- 轮播模块。
- 榜单模块。
- 作者模块。
- VIP 模块。
- 广告位。
- 自定义 HTML。
- 主题组件。
- 拖拽排序。

页面结构保存为 JSON Schema，前台由服务端 renderer 渲染。

### 12.3 小工具与区域

主题可声明区域：

- 首页区域。
- 侧边栏。
- 文章底部。
- 页脚。
- 用户中心。

后台可向区域放置小工具：

- 最新内容。
- 热门内容。
- 分类。
- 标签云。
- 作者榜。
- 广告。
- 友情链接。

---

## 十三、插件系统

### 13.1 插件定位

首版实现插件机制和安全边界，允许后续通过插件扩展业务。

插件能力：

- 上传插件包。
- 启用 / 禁用插件。
- 插件配置页。
- 插件事件监听。
- 插件迁移。
- 插件权限声明。
- 插件版本兼容检查。

### 13.2 插件包结构

```text
plugin.json
src/
database/migrations/
resources/views/
assets/
```

`plugin.json` 示例：

```json
{
  "name": "Example Plugin",
  "slug": "example-plugin",
  "version": "1.0.0",
  "author": "zfy-blog",
  "compatible": "^3.0",
  "provider": "Plugins\\Example\\ExampleServiceProvider",
  "permissions": ["content.read"],
  "events": ["content.published"]
}
```

### 13.3 钩子系统

预留事件：

- `content.created`
- `content.published`
- `content.updated`
- `content.deleted`
- `order.paid`
- `vip.activated`
- `download.created`
- `comment.created`
- `user.registered`

插件不得直接修改核心表结构，必须通过独立迁移和声明式权限接入。

---

## 十四、公开 REST API

### 14.1 API 规范

API 前缀：

```text
/api/v1
```

认证：

- 游客接口允许匿名访问。
- 用户接口使用 Sanctum Token。
- 管理接口需要管理员 Token 和权限。

统一响应：

```json
{
  "success": true,
  "data": {},
  "message": "ok",
  "meta": {}
}
```

错误响应：

```json
{
  "success": false,
  "error": {
    "code": "FORBIDDEN",
    "message": "权限不足"
  }
}
```

### 14.2 核心接口

内容：

```text
GET    /api/v1/contents
GET    /api/v1/contents/{slug}
POST   /api/v1/contents
PATCH  /api/v1/contents/{id}
DELETE /api/v1/contents/{id}
```

分类 / 标签：

```text
GET /api/v1/categories
GET /api/v1/tags
```

媒体：

```text
GET  /api/v1/media
POST /api/v1/media
```

评论：

```text
GET  /api/v1/contents/{id}/comments
POST /api/v1/contents/{id}/comments
POST /api/v1/comments/{id}/like
POST /api/v1/comments/{id}/report
```

商业化：

```text
POST /api/v1/orders
GET  /api/v1/orders
GET  /api/v1/orders/{id}
POST /api/v1/payments/{gateway}/notify
POST /api/v1/wallet/recharge
GET  /api/v1/wallet/transactions
GET  /api/v1/points/transactions
```

下载：

```text
GET  /api/v1/downloads/{content}
POST /api/v1/downloads/{content}/verify
```

主题 / 插件：

```text
GET /api/v1/themes
GET /api/v1/plugins
```

### 14.3 API 安全

- 所有写接口限流。
- Token 可设置名称、能力、过期时间。
- 管理接口必须校验权限点。
- 下载接口不得返回真实私有路径。
- 支付回调接口单独限流和签名校验。
- API 版本不可直接破坏旧客户端。

---

## 十五、安装、升级与部署

### 15.1 Web 安装器

首次访问未安装系统时进入安装器：

1. 环境检查：PHP、扩展、目录权限。
2. 数据库配置：MySQL 主机、端口、库名、用户、密码。
3. Redis 配置。
4. 站点信息：站点名、域名、语言。
5. 创建超级管理员。
6. 执行迁移和 seed。
7. 写入 `.env`。
8. 锁定安装器。

### 15.2 升级中心

升级中心能力：

- 当前版本检测。
- 远程版本检查。
- 数据库迁移。
- 文件完整性检查。
- 升级前备份提醒。
- 升级日志。
- 失败回滚预案。

### 15.3 宝塔单机部署

生产推荐：

- Nginx
- PHP-FPM 8.3+
- MySQL 8
- Redis
- Supervisor
- Meilisearch 可选
- 定时任务：`php artisan schedule:run`
- 队列进程：`php artisan queue:work`

### 15.4 Docker Compose

开发 / 测试提供：

- app
- nginx
- mysql
- redis
- meilisearch optional
- mailpit

### 15.5 环境变量

核心 `.env`：

```env
APP_NAME=zfy-blog
APP_ENV=production
APP_KEY=
APP_URL=

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=zfy_blog
DB_USERNAME=zfy_blog
DB_PASSWORD=

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

SCOUT_DRIVER=database
MEILISEARCH_HOST=
MEILISEARCH_KEY=

FILESYSTEM_DISK=local
QUEUE_CONNECTION=redis
CACHE_STORE=redis
SESSION_DRIVER=database
```

---

## 十六、安全与风控

### 16.1 基础安全

- 密码使用 Laravel Hash。
- 管理后台支持二次验证预留。
- 登录失败限流。
- 注册限流。
- 邮件验证码限流。
- API Token 能力限制。
- CSRF 防护。
- XSS 清洗。
- SQL 注入由 Eloquent / Query Builder 防护。
- 敏感配置加密存储。

### 16.2 内容风控

- 敏感词库。
- 投稿审核。
- 评论审核。
- 用户举报。
- 内容举报。
- 黑名单。
- 用户封禁。
- 作者封禁。
- 操作日志。

### 16.3 资源风控

- 防盗链。
- 下载签名 URL。
- 每日下载次数。
- IP 限制。
- 设备限制。
- 下载日志。
- 私有文件不暴露真实路径。
- 资源水印预留。

### 16.4 支付风控

- 支付回调签名验证。
- 订单幂等处理。
- 金额二次校验。
- 主动查单。
- 异常订单标记。
- 支付日志。
- 退款日志。

---

## 十七、数据库模块草案

核心表按模块拆分：

用户与权限：

- `users`
- `roles`
- `permissions`
- `model_has_roles`
- `model_has_permissions`
- `author_profiles`
- `login_logs`

内容：

- `contents`
- `categories`
- `tags`
- `content_tag`
- `content_images`
- `content_downloads`
- `content_access_rules`
- `comments`
- `comment_reports`

媒体：

- `media`
- `media_folders`
- `attachments`
- `download_logs`

商业化：

- `orders`
- `order_items`
- `payments`
- `payment_logs`
- `wallets`
- `wallet_transactions`
- `points_accounts`
- `points_transactions`
- `points_store_items`
- `points_exchange_orders`

VIP：

- `vip_levels`
- `user_vips`
- `vip_orders`

作者收益：

- `author_earnings`
- `author_withdrawals`
- `author_settlement_rules`

外观与扩展：

- `themes`
- `theme_settings`
- `menus`
- `menu_items`
- `pages_layouts`
- `widgets`
- `plugins`
- `plugin_settings`

系统：

- `settings`
- `notifications`
- `notification_reads`
- `audit_logs`
- `system_versions`
- `upgrade_logs`
- `security_events`

---

## 十八、测试计划

### 18.1 内容测试

- 创建 `post / images / files / page`。
- 草稿、待审、发布、私密、定时、回收站。
- 分类、标签、封面、SEO。
- 搜索索引同步。
- 页面构建器渲染。

### 18.2 编辑器测试

- 块编辑器保存和回显。
- 富文本模式切换。
- Markdown 模式切换。
- 复杂块保存。
- HTML 渲染。
- XSS 清洗。
- 图片、视频、音频、附件上传。
- 隐藏内容权限裁剪。

### 18.3 商业化测试

- VIP 购买。
- 单内容购买。
- 余额充值。
- 余额支付。
- 积分抵扣。
- 积分商城兑换。
- 支付回调幂等。
- 主动查单。
- 订单超时关闭。

### 18.4 权限测试

- 游客。
- 登录用户。
- VIP。
- 多级 VIP。
- 作者。
- 编辑。
- 管理员。
- 超级管理员。
- API Token 权限。

### 18.5 下载测试

- 登录可见。
- VIP 可见。
- 购买可见。
- 评论可见。
- 密码可见。
- 每日次数限制。
- 防盗链。
- 签名 URL。
- 下载日志。

### 18.6 作者测试

- 作者申请。
- 投稿。
- 审核。
- 拒绝。
- 发布。
- 收益分成。
- 提现申请。
- 后台人工打款确认。

### 18.7 主题与插件测试

- 上传 zip。
- 路径穿越拦截。
- 版本兼容检查。
- 启用 / 禁用。
- 配置保存。
- 插件权限声明。
- 恶意文件拦截。

### 18.8 部署测试

- 宝塔部署。
- Docker Compose 启动。
- MySQL 迁移。
- Redis 缓存。
- Meilisearch 可选启用。
- 安装器。
- 升级中心。
- 队列和定时任务。

---

## 十九、里程碑

| 阶段 | 名称 | 主要产出 |
| --- | --- | --- |
| M0 | 项目初始化 | Laravel 项目、环境配置、基础目录、代码规范 |
| M1 | 安装器与基础系统 | Web 安装器、设置中心、用户、角色、权限 |
| M2 | 内容核心 | 统一内容模型、分类、标签、状态流、前台详情 |
| M3 | 编辑器 | TipTap 三模式编辑器、复杂块、HTML 渲染、安全清洗 |
| M4 | 媒体库 | 上传、文件夹、对象存储、本地存储、附件选择器 |
| M5 | 前台主题 | 默认主题、综合信息流、频道页、详情页、菜单 |
| M6 | 评论互动 | 评论、回复、点赞、收藏、举报、通知 |
| M7 | VIP 与权限 | 多级 VIP、内容权限、隐藏内容、下载权限 |
| M8 | 商业化 | 订单、支付网关、余额、积分、积分商城 |
| M9 | 多作者 | 作者申请、投稿审核、收益分成、提现 |
| M10 | REST API | `/api/v1`、Sanctum、API 文档、限流 |
| M11 | 主题与页面构建器 | 主题上传、页面构建器、组件渲染 |
| M12 | 插件系统 | 插件包、启停、钩子、权限、安全校验 |
| M13 | 搜索与风控 | MySQL 搜索、Meilisearch、敏感词、审计日志 |
| M14 | 部署与升级 | 宝塔文档、Docker Compose、升级中心、备份方案 |
| M15 | 总测与发布 | 全链路测试、修复、性能优化、正式发布 |

---

## 二十、交付物

最终交付：

- Laravel 12 完整项目源码。
- MySQL 数据库迁移和 seed。
- Web 安装器。
- 后台管理系统。
- 前台默认主题。
- TipTap 三模式编辑器。
- 媒体库与对象存储配置。
- 公开 REST API 与 Sanctum 认证。
- VIP、付费内容、钱包、积分、积分商城。
- 支付宝官方、微信官方、虎皮椒 V3、易支付网关。
- 多作者、投稿审核、收益分成、人工提现。
- 主题上传、页面构建器、插件系统。
- 宝塔部署文档。
- Docker Compose 开发 / 测试环境。
- 测试用例与上线检查清单。

---

## 二十一、已确认决策

| 项 | 决策 |
| --- | --- |
| 项目名 | zfy-blog |
| 项目类型 | 综合 CMS / 内容商业化平台 |
| 技术栈 | Laravel + PHP + MySQL + Redis |
| 渲染方式 | Blade 服务端渲染 |
| 后台交互 | Livewire |
| API | 公开 REST API，使用 Sanctum |
| 搜索 | 默认 MySQL FULLTEXT + Redis，可选 Meilisearch |
| 内容类型 | `post / images / files / page` |
| 媒体库 | 独立管理所有文件资产 |
| 编辑器 | TipTap / ProseMirror，块编辑 + 富文本 + Markdown |
| 用户体系 | 账号互动 + 多作者平台 |
| 后台角色 | SUPER_ADMIN / ADMIN / EDITOR / USER |
| VIP | 多级 VIP，独立于后台角色 |
| 支付 | 支付宝官方、微信官方、虎皮椒 V3、易支付 |
| 钱包积分 | 余额钱包 + 积分奖励 / 抵扣 / 积分商城 |
| 作者收益 | 平台统一收款，作者分成，后台人工提现 |
| 下载权限 | 登录、VIP、购买、评论、密码、次数、防盗链 |
| 菜单 | 类 WordPress 自定义菜单 |
| 主题 | 支持上传主题包 |
| 页面构建器 | 支持完整页面搭建 |
| 插件 | 支持插件系统和钩子 |
| 通知 | 站内信 + 邮件，微信通知可选 |
| 部署 | 宝塔单机 + Docker Compose |
| 安装升级 | Web 安装器 + 升级中心 |

> 本文档作为 zfy-blog v3 从零开发的依据。后续实现应以本文的领域边界、接口约定、权限规则和测试计划为准。
