# zfy-blog 使用教程

本文档覆盖本地运行、首次安装、宝塔部署、1Panel 部署、Docker Compose 依赖服务、上线维护和常见问题。生产环境部署前请先阅读“上线前检查清单”。

## 目录

- [项目说明](#项目说明)
- [环境要求](#环境要求)
- [本地开发运行](#本地开发运行)
- [首次安装方式](#首次安装方式)
- [宝塔面板部署](#宝塔面板部署)
- [1Panel 部署](#1panel-部署)
- [Docker Compose 部署依赖服务](#docker-compose-部署依赖服务)
- [上线前检查清单](#上线前检查清单)
- [生产环境配置建议](#生产环境配置建议)
- [更新升级流程](#更新升级流程)
- [备份与恢复](#备份与恢复)
- [常见问题](#常见问题)

## 项目说明

`zfy-blog` 是 Laravel 12 + Vue 3 + MySQL + Redis 的综合 CMS / 内容商业化平台，内置文章、图集、资源、独立页面、多作者、VIP、付费内容、积分、钱包、支付、主题、插件和页面构建器。

项目默认后台：

- 后台地址：`/admin`
- 默认邮箱：`admin@zfy-blog.test`
- 默认密码：`zfy-blog-123456`

如果通过网页安装向导创建管理员，请以安装向导填写的账号为准。

## 环境要求

推荐生产环境：

- Linux 服务器：Debian 12、Ubuntu 22.04/24.04、Rocky Linux 9、AlmaLinux 9 均可。
- Web 服务：Nginx 或 OpenResty。
- PHP：8.2 或 8.3，推荐 8.3。
- MySQL：8.0 或 8.4。
- Redis：6 或 7。
- Node.js：20 LTS 或 22 LTS。
- Composer：2.x。

PHP 扩展建议启用：

- 必需：`bcmath`、`ctype`、`curl`、`dom`、`fileinfo`、`filter`、`gd`、`intl`、`json`、`mbstring`、`openssl`、`pdo_mysql`、`session`、`sodium`、`tokenizer`、`xml`、`zip`
- 推荐：`redis`、`opcache`、`pcntl`

目录权限要求：

- `storage/` 可写
- `bootstrap/cache/` 可写
- `.env` 首次安装时可写

## 本地开发运行

Windows 本地开发示例使用 PowerShell 7：

```powershell
pwsh -c "Copy-Item .env.example .env"
pwsh -c "php artisan key:generate"
pwsh -c "composer install"
pwsh -c "npm install"
pwsh -c "npm run build"
pwsh -c "php artisan serve"
```

启动后访问：

- 安装向导：`http://127.0.0.1:8000/install`
- 前台首页：`http://127.0.0.1:8000`
- 后台入口：`http://127.0.0.1:8000/admin`

如果本机没有 MySQL，可以临时使用 SQLite，但网页安装向导当前按 MySQL 流程写入配置。SQLite 更适合本地开发手动初始化：

```env
DB_CONNECTION=sqlite
QUEUE_CONNECTION=database
CACHE_STORE=database
REDIS_CLIENT=predis
ZFY_INSTALLED=true
```

并创建数据库文件：

```powershell
pwsh -c "New-Item -ItemType File -Force database/database.sqlite"
pwsh -c "php artisan migrate --seed"
pwsh -c "php artisan tinker"
```

进入 Tinker 后执行：

```php
app(App\Services\Install\InstallationState::class)->markInstalled(['method' => 'local-sqlite']);
```

## 首次安装方式

推荐使用网页安装向导，因为它会同时完成数据库迁移、核心初始化、管理员创建、`.env` 写入和安装锁定。

### 方式一：网页安装向导

1. 复制 `.env.example` 为 `.env`。
2. 配好 `APP_URL`、数据库、缓存、队列等基础变量。
3. 执行依赖安装和前端构建。
4. 将站点运行目录指向项目的 `public/`。
5. 浏览器访问 `/install`。
6. 按页面填写数据库、站点和管理员信息。
7. 安装完成后访问 `/admin` 登录后台。

生产环境常用命令：

```bash
composer install --no-dev --optimize-autoloader
npm ci
npm run build
php artisan key:generate --force
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

### 方式二：命令行初始化

命令行方式适合自动化部署。执行迁移和 Seeder 后，还需要设置 `ZFY_INSTALLED=true` 并写入安装锁。

```bash
cp .env.example .env
php artisan key:generate --force
php artisan migrate --seed --force
php artisan tinker --execute="app(App\Services\Install\InstallationState::class)->markInstalled(['method' => 'cli']);"
```

`.env` 中至少确认：

```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://你的域名
ZFY_INSTALLED=true

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=zfy_blog
DB_USERNAME=zfy_blog
DB_PASSWORD=强密码

QUEUE_CONNECTION=database
CACHE_STORE=redis
SESSION_DRIVER=database
REDIS_CLIENT=predis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
```

## 宝塔面板部署

以下步骤按“PHP 项目 + Nginx + MySQL + Redis”部署。

### 1. 安装运行环境

在宝塔面板的软件商店安装：

- Nginx
- MySQL 8.0 或 8.4
- Redis
- PHP 8.2 或 8.3
- Composer
- Node.js 20 或 22

进入 PHP 设置，启用上文列出的扩展。生产环境建议开启 OPcache。

### 2. 创建数据库

在“数据库”中新建：

- 数据库名：`zfy_blog`
- 用户名：`zfy_blog`
- 密码：使用强密码
- 字符集：`utf8mb4`

记录数据库名、用户名和密码，后续写入 `.env` 或安装向导。

### 3. 上传或拉取代码

推荐目录：

```text
/www/wwwroot/zfy-blog
```

如果使用 Git：

```bash
cd /www/wwwroot
git clone 你的仓库地址 zfy-blog
cd /www/wwwroot/zfy-blog
```

如果使用压缩包上传，解压后确认 `artisan`、`composer.json`、`public/index.php` 位于项目根目录。

### 4. 安装后端依赖

在宝塔终端进入项目目录：

```bash
cd /www/wwwroot/zfy-blog
composer install --no-dev --optimize-autoloader
```

如果系统默认 `php` 不是宝塔 PHP 版本，可使用宝塔 PHP 绝对路径，例如：

```bash
/www/server/php/83/bin/php /usr/bin/composer install --no-dev --optimize-autoloader
```

具体 PHP 目录以服务器实际安装版本为准。

### 5. 构建前端资源

```bash
cd /www/wwwroot/zfy-blog
npm ci
npm run build
```

构建成功后应生成 `public/build/manifest.json`。

### 6. 配置环境文件

```bash
cp .env.example .env
php artisan key:generate --force
```

建议 `.env`：

```env
APP_NAME=zfy-blog
APP_ENV=production
APP_DEBUG=false
APP_URL=https://你的域名

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=zfy_blog
DB_USERNAME=zfy_blog
DB_PASSWORD=强密码

CACHE_STORE=redis
SESSION_DRIVER=database
QUEUE_CONNECTION=database
REDIS_CLIENT=predis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
```

如果准备使用网页安装向导，先保持：

```env
ZFY_INSTALLED=false
```

如果使用命令行初始化，初始化完成后改为：

```env
ZFY_INSTALLED=true
```

### 7. 配置站点运行目录

在宝塔“网站”中添加 PHP 站点：

- 域名：你的域名
- 根目录：`/www/wwwroot/zfy-blog/public`
- PHP 版本：PHP 8.2 或 8.3

不要把网站根目录指向 `/www/wwwroot/zfy-blog`，否则 `.env`、源码和配置文件可能暴露。

### 8. 配置伪静态

宝塔站点设置中选择 Laravel 伪静态；如果没有模板，使用：

```nginx
location / {
    try_files $uri $uri/ /index.php?$query_string;
}

location ~ /\.(?!well-known).* {
    deny all;
}
```

如果上传大图片或资源，可在 Nginx 配置中适当增加：

```nginx
client_max_body_size 50m;
```

### 9. 设置目录权限

```bash
cd /www/wwwroot/zfy-blog
chown -R www:www storage bootstrap/cache
chmod -R 775 storage bootstrap/cache
```

如果面板运行用户不是 `www`，按实际 PHP-FPM 用户调整。

### 10. 完成安装

推荐访问：

```text
https://你的域名/install
```

按页面填写数据库、站点、管理员信息并完成安装。

如果使用命令行初始化：

```bash
php artisan migrate --seed --force
php artisan tinker --execute="app(App\Services\Install\InstallationState::class)->markInstalled(['method' => 'bt']);"
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

### 11. 配置队列守护

安装宝塔“进程守护管理器”或 Supervisor 插件，新增进程：

- 名称：`zfy-blog-queue`
- 运行目录：`/www/wwwroot/zfy-blog`
- 启动命令：

```bash
php artisan queue:work --sleep=3 --tries=3 --timeout=90
```

如果宝塔 PHP 不在默认 PATH，使用绝对路径：

```bash
/www/server/php/83/bin/php artisan queue:work --sleep=3 --tries=3 --timeout=90
```

### 12. 配置计划任务

宝塔“计划任务”中新增 Shell 脚本，每分钟执行：

```bash
cd /www/wwwroot/zfy-blog && php artisan schedule:run >> /dev/null 2>&1
```

### 13. 开启 HTTPS

在宝塔站点 SSL 中申请证书，开启强制 HTTPS。开启后确认 `.env`：

```env
APP_URL=https://你的域名
```

然后刷新缓存：

```bash
php artisan config:clear
php artisan config:cache
```

## 1Panel 部署

1Panel 适合用“网站 + PHP 运行环境 + 应用商店数据库”方式部署。不同版本界面名称可能略有变化，以面板当前显示为准。

### 1. 安装基础应用

在 1Panel 应用商店或运行环境中准备：

- OpenResty 或 Nginx
- MySQL 8.0/8.4
- Redis 6/7
- PHP 8.2/8.3 运行环境
- Node.js 20/22

PHP 运行环境中启用项目所需扩展，至少包含 `pdo_mysql`、`fileinfo`、`gd`、`intl`、`mbstring`、`openssl`、`sodium`、`zip`。

### 2. 创建数据库

在 1Panel 的 MySQL 应用中创建：

- 数据库：`zfy_blog`
- 用户：`zfy_blog`
- 密码：强密码
- 字符集：`utf8mb4`

如果 PHP 运行环境和 MySQL 在不同容器网络中，数据库主机以 1Panel 提供的连接信息为准。不能确认时，优先使用面板显示的内网地址或暴露端口地址。

### 3. 创建网站

在“网站”中新建 PHP 网站：

- 类型：PHP 运行环境网站
- 运行环境：选择 PHP 8.2/8.3
- 主目录：项目目录
- 运行目录：项目的 `public/`
- 默认文档：`index.php`

关键点：站点对外根目录必须是 `public/`，不是项目根目录。

### 4. 上传代码

方式一：面板文件管理上传压缩包，解压到站点目录。

方式二：终端 Git 拉取：

```bash
cd /opt/1panel/www/sites
git clone 你的仓库地址 zfy-blog
cd zfy-blog
```

实际路径以 1Panel 创建网站时的目录为准。

### 5. 安装依赖和构建

进入项目目录：

```bash
composer install --no-dev --optimize-autoloader
npm ci
npm run build
cp .env.example .env
php artisan key:generate --force
```

如果面板终端的 `php`、`composer`、`node` 不是网站运行环境版本，请在 1Panel 的 PHP 运行环境终端中执行，或使用运行环境提供的绝对路径。

### 6. 配置 `.env`

示例：

```env
APP_NAME=zfy-blog
APP_ENV=production
APP_DEBUG=false
APP_URL=https://你的域名
ZFY_INSTALLED=false

DB_CONNECTION=mysql
DB_HOST=面板提供的数据库主机
DB_PORT=3306
DB_DATABASE=zfy_blog
DB_USERNAME=zfy_blog
DB_PASSWORD=强密码

CACHE_STORE=redis
SESSION_DRIVER=database
QUEUE_CONNECTION=database
REDIS_CLIENT=predis
REDIS_HOST=面板提供的 Redis 主机
REDIS_PORT=6379
```

### 7. 配置伪静态

在网站配置中加入 Laravel 路由规则：

```nginx
location / {
    try_files $uri $uri/ /index.php?$query_string;
}

location ~ /\.(?!well-known).* {
    deny all;
}
```

### 8. 设置权限

在项目目录执行：

```bash
chmod -R 775 storage bootstrap/cache
```

如果 1Panel 支持设置网站用户归属，确保 PHP 运行用户可以写入 `storage/` 和 `bootstrap/cache/`。

### 9. 完成安装

访问：

```text
https://你的域名/install
```

安装完成后进入后台：

```text
https://你的域名/admin
```

### 10. 配置守护和计划任务

队列进程：

```bash
php artisan queue:work --sleep=3 --tries=3 --timeout=90
```

计划任务每分钟执行：

```bash
cd 项目目录 && php artisan schedule:run >> /dev/null 2>&1
```

在 1Panel 中可用“进程守护”和“计划任务”分别配置。

## Docker Compose 部署依赖服务

当前仓库的 `docker-compose.yml` 提供 MySQL、Redis、Meilisearch 三个依赖服务，不包含 PHP 应用容器和 Nginx 容器。推荐用它做本地开发或面板部署时的依赖服务。

### 1. 启动依赖服务

Windows PowerShell：

```powershell
pwsh -c "docker compose up -d"
pwsh -c "docker compose ps"
```

Linux/macOS：

```bash
docker compose up -d
docker compose ps
```

当前服务端口：

- MySQL：`127.0.0.1:3306`
- Redis：`127.0.0.1:6379`
- Meilisearch：`127.0.0.1:7700`

当前 MySQL 默认值来自 `docker-compose.yml`：

```env
DB_DATABASE=zfy_blog
DB_USERNAME=root
DB_PASSWORD=root
```

### 2. 配置项目 `.env`

```env
APP_URL=http://127.0.0.1:8000
ZFY_INSTALLED=false

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=zfy_blog
DB_USERNAME=root
DB_PASSWORD=root

CACHE_STORE=redis
SESSION_DRIVER=database
QUEUE_CONNECTION=database
REDIS_CLIENT=predis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
```

如需使用 Meilisearch：

```env
SCOUT_DRIVER=meilisearch
MEILISEARCH_HOST=http://127.0.0.1:7700
MEILISEARCH_KEY=local-meili-key
```

默认 `.env.example` 使用 `SCOUT_DRIVER=database`，不启用 Meilisearch 也可以正常运行。

### 3. 启动应用

Windows PowerShell：

```powershell
pwsh -c "composer install"
pwsh -c "npm install"
pwsh -c "npm run build"
pwsh -c "php artisan key:generate"
pwsh -c "php artisan serve"
```

然后访问：

```text
http://127.0.0.1:8000/install
```

### 4. 停止依赖服务

```powershell
pwsh -c "docker compose down"
```

如果要删除 MySQL 数据卷：

```powershell
pwsh -c "docker compose down -v"
```

注意：`down -v` 会删除数据库数据，执行前必须确认已经备份。

### 5. 全容器化部署说明

当前项目未内置应用 Dockerfile。如果要生产环境全容器化，建议拆成：

- `nginx` 或 `openresty`：只暴露 `public/`
- `php-fpm`：运行 Laravel 应用
- `queue`：运行 `php artisan queue:work`
- `scheduler`：运行定时任务或由宿主机 cron 调度
- `mysql`
- `redis`

在全容器网络中，`.env` 应使用服务名：

```env
DB_HOST=mysql
REDIS_HOST=redis
MEILISEARCH_HOST=http://meilisearch:7700
```

全容器化前请确认上传目录、日志目录和数据库目录都有持久化卷。

## 上线前检查清单

正式对外开放前逐项确认：

- 域名 DNS 已解析到服务器。
- HTTPS 证书已签发并启用。
- `.env` 中 `APP_URL` 使用真实 HTTPS 域名。
- `.env` 中 `APP_ENV=production`、`APP_DEBUG=false`。
- 网站运行目录指向项目的 `public/`。
- PHP 版本和扩展满足要求。
- MySQL、Redis 连接正常。
- `storage/` 和 `bootstrap/cache/` 可写。
- 已执行 `composer install --no-dev --optimize-autoloader`。
- 已执行 `npm ci` 和 `npm run build`。
- 已完成 `/install` 或命令行初始化。
- 安装完成后 `.env` 中 `ZFY_INSTALLED=true`。
- `storage/app/zfy/install.lock` 存在。
- 队列守护已运行。
- 计划任务已配置为每分钟执行。
- 数据库和 `storage/app/` 已配置定时备份。
- 支付平台回调地址已配置为线上 HTTPS 地址。

## 生产环境配置建议

上线前检查 `.env`：

```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://你的域名
ZFY_INSTALLED=true
CACHE_STORE=redis
SESSION_DRIVER=database
QUEUE_CONNECTION=database
```

缓存优化：

```bash
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

权限检查：

```bash
php artisan about
php artisan route:list
php artisan migrate:status
```

安全建议：

- 网站根目录必须指向 `public/`。
- 不要提交 `.env`、支付密钥、数据库密码和私钥。
- 生产环境关闭 `APP_DEBUG`。
- HTTPS 开启后同步更新 `APP_URL`。
- 支付回调域名必须和支付平台配置一致。
- 队列守护和计划任务必须常驻，否则部分异步任务不会执行。

## 更新升级流程

建议每次升级前先备份数据库和 `storage/`。

```bash
cd 项目目录
git pull
composer install --no-dev --optimize-autoloader
npm ci
npm run build
php artisan migrate --force
php artisan config:clear
php artisan route:clear
php artisan view:clear
php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan queue:restart
```

如果使用 PHP-FPM，可在面板中重载 PHP 服务或执行：

```bash
php artisan optimize:clear
```

## 备份与恢复

必须备份：

- MySQL 数据库
- `.env`
- `storage/app/`
- `storage/logs/` 可按需备份
- 自定义主题、插件和上传资源

宝塔和 1Panel 都可以在面板内设置数据库定时备份。也可以使用命令：

```bash
mysqldump -u zfy_blog -p zfy_blog > zfy_blog.sql
tar -czf zfy_storage.tar.gz storage/app
```

恢复示例：

```bash
mysql -u zfy_blog -p zfy_blog < zfy_blog.sql
tar -xzf zfy_storage.tar.gz
php artisan optimize:clear
```

## 常见问题

### 1. 首页一直跳转到 `/install`

说明系统还没有被标记为已安装。检查：

- `.env` 中 `ZFY_INSTALLED=true`
- `storage/app/zfy/install.lock` 是否存在

命令行补锁：

```bash
php artisan tinker --execute="app(App\Services\Install\InstallationState::class)->markInstalled(['method' => 'manual']);"
```

### 2. 访问首页 404 或路由不生效

检查网站运行目录是否指向 `public/`，并确认伪静态存在：

```nginx
location / {
    try_files $uri $uri/ /index.php?$query_string;
}
```

### 3. 页面报 Vite manifest not found

说明前端资源没有构建：

```bash
npm ci
npm run build
```

确认存在：

```text
public/build/manifest.json
```

### 4. 上传图片失败

检查：

- `storage/` 是否可写
- PHP `fileinfo`、`gd` 扩展是否启用
- Nginx `client_max_body_size` 是否足够
- `.env` 的 `APP_URL` 是否正确

### 5. 数据库连接失败

检查 `.env`：

- 宝塔/1Panel 本机 PHP 连接面板数据库，通常用面板提供的主机和端口。
- 当前 Docker Compose 依赖服务中，宿主机连接 MySQL 用 `127.0.0.1:3306`、用户名 `root`、密码 `root`。
- 全容器化时应用容器连接 MySQL 用服务名 `mysql`。

修改 `.env` 后清缓存：

```bash
php artisan config:clear
```

### 6. 后台登录后没有权限

确认 Seeder 已执行：

```bash
php artisan db:seed --class=Database\\Seeders\\CoreInstallSeeder --force
```

默认超级管理员：

```text
admin@zfy-blog.test
zfy-blog-123456
```

如果通过安装向导创建管理员，以安装向导账号为准。

### 7. 队列任务不执行

确认队列进程正在运行：

```bash
php artisan queue:work --sleep=3 --tries=3 --timeout=90
```

生产环境建议用宝塔进程守护、1Panel 进程守护或 Supervisor 管理。

### 8. 配置改了但不生效

Laravel 配置被缓存时，需要刷新：

```bash
php artisan optimize:clear
php artisan config:cache
```

### 9. 支付回调异常

检查：

- `APP_URL` 是否为真实 HTTPS 域名。
- 支付平台回调地址是否指向当前域名。
- 服务器安全组、防火墙是否允许公网访问。
- `.env` 或后台支付配置中的密钥是否正确。

回调路由：

```text
POST /payments/{gateway}/notify
```

### 10. 生产环境不建议暴露的文件

只允许 Web 服务器暴露 `public/`。不要暴露：

- `.env`
- `storage/`
- `vendor/`
- `database/`
- `composer.json`
- `package.json`
- `resources/`
